server {
  listen 8080;

  access_log /dev/stdout;
  error_log /dev/stderr;

  resolver 127.0.0.11 valid=30s ipv6=off;

  location = /health {
    add_header Content-Type text/plain;
    return 200 "ok";
  }

  location = /api/health {
    set $backend "incident-management:8002";
    proxy_pass http://$backend/health;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_intercept_errors on;
    error_page 502 503 504 = @backend_unavailable;
  }

  # On-call service routes
  location /api/v1/oncall/ {
    set $backend "oncall-service:8003";
    proxy_pass http://$backend/api/v1/oncall/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_intercept_errors on;
    error_page 502 503 504 = @backend_unavailable;
  }

  location /api/v1/schedules {
    set $backend "oncall-service:8003";
    proxy_pass http://$backend/api/v1/schedules;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_intercept_errors on;
    error_page 502 503 504 = @backend_unavailable;
  }

  location /api/v1/escalate {
    set $backend "oncall-service:8003";
    proxy_pass http://$backend/api/v1/escalate;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_intercept_errors on;
    error_page 502 503 504 = @backend_unavailable;
  }

  # Alert ingestion routes
  location /api/v1/alerts {
    set $backend "alert-ingestion:8001";
    proxy_pass http://$backend/api/v1/alerts;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_intercept_errors on;
    error_page 502 503 504 = @backend_unavailable;
  }

  # Incident management routes (default)
  location /api/ {
    set $backend "incident-management:8002";
    proxy_pass http://$backend/api/;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_intercept_errors on;
    error_page 502 503 504 = @backend_unavailable;
  }

  location @backend_unavailable {
    add_header Content-Type application/json;
    return 503 '{"error":"Backend service unavailable"}';
  }

  location / {
    root /usr/share/nginx/html;
    try_files $uri $uri/ /index.html;
  }
}
